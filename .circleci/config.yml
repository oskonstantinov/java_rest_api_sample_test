version: 2.1

jobs:
  build:

    working_directory: ~/repo
    
    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:
      - checkout
      - run: ./gradlew dependencies
      - run: ./gradlew test