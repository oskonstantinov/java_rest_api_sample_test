version: 2.1

jobs:
  build:

    working_directory: ~/java_rest_api_sample_test/rest-api-sample

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:
      - checkout

      - run: gradle build

      - run: gradle test